name: dogs1
description: A Dog Adoption App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_lints: ^5.0.0
  http: ^0.13.0  # Ensure you have this to fetch data from API

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
